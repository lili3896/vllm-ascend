# {算子名称} 设计文档

# 一、需求

## 1.1 需求背景和描述

*采用 5W2H 方法描述：什么产品/客户、遇到什么困难或困惑、提出什么需求。*

{需求背景描述}

## 1.2 验收标准

### 1.2.1 精度标准

* **标杆算子:** {以 CPU/GPU/NPU 的 xx 算子作为精度比较基准；若无对应算子，以 golden 实现为准}
* **精度通过标准**：{单标杆高性能/单标杆高精度等}
* **验收范围**：{业务 shape 或泛化 shape，泛化范围约束}

### 1.2.2 性能标准

* **性能基线**: {以 CPU/GPU/NPU 的 xx 算子作为性能比较基准；无可对比算子则填"无"}
* **性能通过标准**：{端到端或 device 性能提升比例/目标，部分场景需描述 shape 范围}
* **验收范围**：{业务 shape 或泛化 shape}

## 1.3 业务 shape

{业务 shape 列表}

# 二、约束和周边影响评估

## 2.1 周边依赖

1. docker 镜像
   {docker 镜像地址}
2. CANN
   {CANN CMC 版本链接}
3. PyTorch
   {PyTorch CMC 链接}

附：昇腾云各版本配套 docker 镜像
{昇腾云 docker 镜像链接}

## 2.2 支持芯片型号

{如：昇腾 910B、910C}

# 三、算子功能及定义

## 3.1 算子功能分析

* 算子功能：{算子功能描述}
* 计算公式：{数学公式}

{量化流程描述}

## 3.2 参数说明

| 参数名 | 参数描述 | 可选/必选 | 数据类型 | 数据格式 | 维度 | 值域 | 是否支持非连续张量 | 是否有数据对齐要求 | 是否支持空 tensor | 异常值域 nan/inf/-inf |
|--------|---------|----------|---------|---------|------|------|----------------|----------------|--------------|------------------|
| {参数1} | {描述} | {必选/可选} | {类型} | {ND/NCHW等} | {维度} | {值域} | {支持/不支持} | {对齐要求} | {支持/不支持} | {支持/不支持} |
| {参数2} | {描述} | {必选/可选} | {类型} | {ND/NCHW等} | {维度} | {值域} | {支持/不支持} | {对齐要求} | {支持/不支持} | {支持/不支持} |

**其他约束**：

* **约束1**: {描述}
* **约束2**: {描述}

## 3.3 其他算子功能支持

* 是否支持图模式：{是/否}
* 是否支持确定性计算：{是/否}
* 是否涉及反向测试：{是/否}

### 3.3.2 算子原型定义

{算子原型定义}

### 3.3.3 接口定义

* aclnn 接口定义：

```cpp
aclnnStatus aclnn{OpName}GetWorkspaceSize(
    const aclTensor *{param1},
    {params...},
    uint64_t *workspaceSize,
    aclOpExecutor **executor);
```

```cpp
aclnnStatus aclnn{OpName}(
    void *workspace,
    uint64_t workspaceSize,
    aclOpExecutor *executor,
    aclrtStream stream);
```

* pybind 接口定义：

```cpp
{pybind 接口声明}
```

# 四、详细方案设计

## 4.1 计算逻辑

{整体计算逻辑描述，包括核间核内循环结构}

## 4.2 Tiling 方案

### 多核切分策略

| 项目 | 描述 |
|------|------|
| 切分维度 | {按哪个维度切分} |
| 每核处理量 | {每个 AI Core 处理的数据量} |
| AI Core 数量 | {使用的 AI Core 数量} |

### UB 切分策略

| 项目 | 描述 |
|------|------|
| 单次处理数据量 | {tileLength} |
| 是否分 chunk | {是/否} |
| chunk 大小计算公式 | {chunkSize 计算公式} |

### Buffer 规划

| Buffer 名称 | 用途 | 大小计算公式 |
|------------|------|-------------|
| {inputBuf} | {输入 buffer} | {tileLength * sizeof(T)} |
| {outputBuf} | {输出 buffer} | {tileLength * sizeof(T)} |
| {tmpBuf} | {临时计算 buffer} | {大小} |

### 分支场景覆盖

| 分支 | 条件 | 处理策略 |
|------|------|---------|
| {分支1} | {条件} | {策略} |
| {分支2} | {条件} | {策略} |

### TilingData 结构体

```cpp
struct {OpName}TilingData {
    uint64_t totalLength;
    uint32_t tileLength;
    // {其他字段}
};
```

## 4.3 Kernel 方案

### 模板划分

| 模板 | 触发条件 | 模板参数 | 适用场景 |
|-----|---------|---------|---------|
| 模板一 | {条件} | {参数} | {场景} |
| 模板二 | {条件} | {参数} | {场景} |

### API 映射

| 计算步骤 | Ascend C API | 参数签名 | 约束说明 |
|---------|-------------|---------|---------|
| 数据搬入 | {API} | {参数} | {约束} |
| {计算步骤} | {API} | {参数} | {约束} |
| 数据搬出 | {API} | {参数} | {约束} |

### 数据流

```text
1. DataCopy: GM({input}) -> UB({inputBuf})
2. Compute:  UB({inputBuf}) -> UB({outputBuf})
3. DataCopy: UB({outputBuf}) -> GM({output})
```

### 内存管理

| 内存区域 | 大小计算 | 说明 |
|---------|---------|------|
| 输入 UB | {大小} | {说明} |
| 输出 UB | {大小} | {说明} |
| 临时缓冲区 | {大小} | {说明} |
| Workspace | {大小} | {说明} |

# 五、测试策略

## 5.1 测试参数说明

业务 shape：
{业务 shape 列表}

泛化 shape：
{泛化 shape 范围}

range 值域（是否支持异常值域）：
{值域描述}

## 5.2 测试脚本

### 5.2.1 CPU golden 实现

{是否使用 CPU golden 实现}

### 5.2.2 NPU 小算子实现

{是否使用 NPU 小算子实现}
